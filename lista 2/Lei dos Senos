import math
lado1=int(input('Insira o lado 1\n'))
lado2=int(input('Insira o lado 2\n'))
ang=float(input('Insira o angulo em graus\n'))
soma=pow(lado1,2) + pow(lado2,2)
prod=2*lado1*lado2*math.cos(ang)
resp= math.sqrt(soma-prod)
print('terceiro lado - {0:.5f}' .format(resp))
